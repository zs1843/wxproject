<view hidden="{{hidden}}">
  <loading>加载中...</loading>
</view>

<view class="game"> 
    <!-- 矩阵 -->
    <view>
      <view class='row' wx:for="{{4}}" wx:for-item="row" wx:key="row.key" >
        <view class='cell' wx:for="{{4}}" wx:for-item="cell" wx:key="cell.key">
          <view class='cell-con cell-con-{{cell}}'>
            {{cell}}
          </view>
        </view>
      </view> 
    </view>
  </view>